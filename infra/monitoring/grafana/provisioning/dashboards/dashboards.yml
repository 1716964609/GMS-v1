apiVersion: 1

providers:
  - name: GMS
    orgId: 1
    folder: GMS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: $GMS_GRAFANA_DASHBOARDS_PATH
